# WORKLOG — Elmenes Coffee Profesyonelleştirme

Branch: `refactor/professional-overhaul` · Plan: `MASTER_TASK_PLAN.md`

Format: `TASK-XXX | tarih | özet | build / tsc | regresyon notu`

---

## FAZ 0 — Analiz & Plan

| Task | Tarih | Özet | build/tsc | Not |
|---|---|---|---|---|
| TASK-000 | 2026-09-01 | Repo tam analizi; audit ile karşılaştırma; ek bulgular E1–E20; art direction; 151 task + 7 backlog; bağımlılık sıralı plan. | ✅ / ✅ (baseline) | Baseline build: Next 16.2.0 Turbopack, TS temiz, 15 route static. |

## FAZ 1 — Kritik UX & kırık akışlar

| Task | Tarih | Özet | build/tsc | Not |
|---|---|---|---|---|
| TASK-001 | 2026-09-01 | Branch açıldı; baseline build doğrulandı; `WORKLOG.md` + `docs/CONVENTIONS.md` oluşturuldu. | ✅ / ✅ | Kod davranışı değişmedi. |
| TASK-002 | 2026-09-01 | Builder scroll-hijack kaldırıldı: `CoffeeRight.tsx` wheel `useEffect` + unused import silindi. `.coffee-right` iç scroll konteyneri (`height:100vh; overflow-y:auto`, gizli scrollbar) kaldırıldı; `.config-section` `min-height:100vh` → `padding:48px 0`. | ✅ / ✅ | Bölümler artık sayfa akışında. Sol panel `fixed` çocukları TASK-003'te düzelecek (geçici görsel bozukluk beklenir). |
| TASK-003 | 2026-09-01 | Builder sol paneli fixed/vh → normal akış. `.lab-badge/.hero-title/.hero-sub/.arena-stats-container/.coffee-name-input-wrapper/.hero-btn/.arena-btn` `position:fixed`+`*vh` kaldırıldı. `.hero-title` `82px`→`clamp(2rem,4vw,2.75rem)`. `.coffee-left` `gap:20px` + `align-self:flex-start` (stretch→içerik boyu). 1024 media query'deki anlamsız `left/bottom` override'ları temizlendi. | ✅ / ✅ | Browser (1440px): sol panel akıyor, çakışma yok, 8 config bölümü render, sayfa normal scroll. `.coffee-left` 589px, `.coffee-right` 4262px. Bölüm içi gevşek dikey boşluk TASK-088/089'da sıkılaşacak. |
| TASK-004 | 2026-09-01 | Builder CTA her zaman görünür + `disabled={!allSelected}` + ilerleme göstergesi. `SELECTION_STEPS` tek kaynak; `selectedCount`/`missingSteps`. "N / 8 seçim tamamlandı" + track/fill bar + "Kalan: …" eksik-adım anchor listesi. `ConfigSection` `<section id="section-{field}">` + `scroll-margin-top:110px`. `.arena-btn:disabled` stili. | ✅ / ✅ | Browser: 0/8 → 1/8 güncelleniyor, fill %12.5, eksik liste güncelleniyor, buton disabled kalıyor, section id'leri anchor için hazır. |
| TASK-005 | 2026-09-01 | Builder seçenek dizileri modül seviyesine (`MILK_OPTIONS`…). `DEFAULT_FORM`: opt-out'lar (Köpük Yok, Şurup/Baharat/Tatlandırıcı İstemiyorum, Değişiklik Yok) + Laktozlu Süt + Brezilya Çekirdeği + Karton Bardak seçili gelir. `ConfigSection` 24px kırmızı `*` + "(Bu kısım zorunludur)" kaldırıldı; `.required-star`/`.required-text` CSS silindi. | ✅ / ✅ | Browser: açılışta 8/8, geçerli reçete, Toplam 130 TL→₺, CTA aktif. Arena kopyalanan tarif yolu `DEFAULT_FORM` ile korunuyor. |
| TASK-006 | 2026-09-01 | Builder şeffaf fiyat dökümü: "Temel kahve ₺100" + ücretli eklentiler satır satır ("Süt Tipi · Laktozlu Süt + ₺10") + varsa "Arena indirimi (%15) − ₺X" + "Toplam". `fmtTRY` geçici `Intl.NumberFormat('tr-TR', TRY)` (TASK-074'te lib'e taşınacak). Yaratıcılık puanı kutusuna 1 cümlelik açıklama. Eski tek satır "{total} TL" kaldırıldı. | ✅ / ✅ | Browser: seçim değişince döküm + toplam + skor anında güncelleniyor (Laktozsuz Süt → +₺15, Toplam ₺135). |
| TASK-007 | 2026-09-01 | Sipariş durum modeli + tipler. `lib/types.ts`: `RecipeOption/CoffeeRecipe/Product/CartLine/Address/DeliveryMethod/OrderStatus/OrderItem/Order/OrderInput`. `lib/orders.ts`: `getOrders/getOrder/createOrder/updateOrderStatus/deleteOrder`, `nextStatus`, `STATUS_FLOW`, `STATUS_LABEL`, `legacyStatusToCanonical`, `parseOrderDate`, `migrateLegacy` (eski `"orders"` → kanonik `elmenes.orders`). SSR guard'lı. | ✅ / — | Henüz hiçbir yerden import edilmiyor; wiring TASK-008/011/122. tsc temiz. |
| TASK-008 | 2026-09-01 | `/siparis` yeniden yazıldı: sonsuz "Hazırlanıyor…" + `setInterval` `dots` + `.siparis-overlay`/blur/keyframe animasyonlar kaldırıldı. Yeni: yeşil onay ikonu + "Siparişin alındı" + "Sipariş no: #A1042" + kalem kalem özet (kalem(ler), ara toplam, indirim, teslimat, toplam) + durum rozeti + ETA metni. `getOrder(?o=)` / son sipariş `lib/orders`. Boş durum: "Görüntülenecek sipariş bulunamadı" + Menüye git. `page.tsx` `<Suspense>` (useSearchParams). `siparis.css` sadeleştirildi (fixed bg → normal akış, tam refactor TASK-090). JSX girintisi düzeltildi. | ✅ / ✅ | Browser: hem dolu (fixture A1042: ₺145 toplam, indirim ₺5) hem boş durum doğrulandı. |
| TASK-009 | 2026-09-01 | Kullanıcıya görünen developer/hata mesajları insancıllaştırıldı. `AuthForm.tsx`: "Server'a ulaşılamıyor. Lütfen backend terminalini kontrol edin!" → "Şu an işlem tamamlanamadı. Lütfen birazdan tekrar dene." · "Bir hata oluştu" → "Bir şeyler ters gitti, tekrar dener misin?" (`RegisterClient` de). `console.error` mesajları korundu (kullanıcı görmez). Grep: JSX/toast metninde "backend/server/terminal" kalmadı. | ✅ / — | tsc temiz; build TASK-010 ile. |
| TASK-010 | 2026-09-01 | Builder "Laboratuvarı Aç" yapay ön adımı kaldırıldı. `started` state ve `setStarted` tamamen silindi; içerik ilk render'da görünür. `CoffeeRight` `started` prop'u kaldırıldı, config bölümleri hep render. `.hero-btn` CSS + kullanılmayan `@keyframes readyPop` + artık no-op olan 768 media query bloğu temizlendi. Locked-banner'daki `🔒` emoji kaldırıldı (lucide Lock zaten var). | ✅ / ✅ | Browser: `/kahveniolustur` açılışta tam builder (stats + fiyat + isim + CTA + 8 bölüm) — ekstra tık yok. Arena kilitli tarif yolu korunuyor. |
| TASK-011 | 2026-09-01 | Builder → sipariş `lib/orders.createOrder()` üzerinden (kanonik `elmenes.orders`, kuruş cinsinden tutarlar, id `A####`). Redirect `/siparis?o=<id>`. Geçiş dönemi: Admin hâlâ eski `"orders"` okuduğu için aynı id ile eski formatta bir kopya da yazılıyor (TASK-122'de kaldırılacak). `coffees` yazımı korundu (profil/arena). Toast metinleri sadeleşti. | ✅ / ✅ | Browser uçtan uca: builder → sipariş → `/siparis?o=A4708`; kanonik kayıt + legacy mirror (aynı id) doğrulandı. Davranış notu: eski dağınık elle yazım → tek veri katmanı; admin görünürlüğü TASK-122'de tamamlanacak. |
| TASK-012 | 2026-09-01 | Arena feed/podium boş durumu. `!isLoading && posts.length===0` → `.arena-empty` bloğu (Coffee ikonu + "Henüz tasarım paylaşılmamış" + açıklama + "Kahveni tasarla" → `/kahveniolustur`). Skeleton mantığı korundu. `.arena-empty` CSS eklendi (token: `--primary-coffee/--primary-hover`). | ✅ / ✅ | Browser: `arenaPosts` temiz → boş durum render, grid render edilmiyor. |
| TASK-013 | 2026-09-01 | Arena sahte sosyal kanıt + tutarsız indirim düzeltildi. `+124` + 3 sabit avatar (`/avatar1-3.jpg`) kaldırıldı → gerçek `participantCount` (benzersiz `userName`), sadece >0 ise "N kişi bu ay tasarım paylaştı". Reward "%20 İndirim Kuponu" → "%15" (builder ile tutarlı). Drawer "Master Barista" sabiti → `arenaScore>=50 ? "Usta Barista" : "Barista"`. `.join-avatars/.join-avatar` CSS silindi, `.join-count` metin stiline döndü. Başlık "SEN DE ARENADA YERİNİ AL!" → "Sen de yerini al". | ✅ / ✅ | tsc + build yeşil. |
| TASK-014 | 2026-09-01 | Arena yanıltıcı ikon düzeltildi. `.copy-recipe-btn` `ShoppingCart` → `ClipboardCopy` (lucide). `aria-label`: "Tarifi Kopyala" → "Tarifi kahve tasarımına kopyala". Toast: "Tarif laboratuvara aktarıldı! %15 indirim kazandın!" → "Tarif kahve tasarımına aktarıldı — %15 indirim uygulandı." | ✅ / — | tsc temiz; build TASK-015 checkpoint'te. |
| TASK-015 | 2026-09-01 | **FAZ 1 REGRESSION CHECKPOINT.** `npx next build` ✅ (15 route static). `npx tsc --noEmit` ✅. `npx eslint app/siparis` temiz; dokunulan dosyalarda yeni `any` yok (lib/orders `unknown`'a çevrildi). Kritik akışlar: builder render + varsayılan reçete + fiyat dökümü + ilerleme + CTA ✅; builder→`createOrder`→`/siparis?o=` onay ✅; `/siparis` dolu+boş ✅; arena boş+dolu, sahte veri yok ✅; menü/profil çökmüyor ✅; auth hata mesajları insancıl ✅. Not: `next dev` konsolunda TASK-010/013 ara-kayıt anlarından kalma bayat HMR parse hataları görünüyor; `next build` + `tsc` + canlı DOM temiz — bayat buffer. Ön mevcut `no-explicit-any` lint hataları (dokunulmamış dosyalar) → TASK-148. **FAZ 1 COMPLETE.** | ✅ / ✅ | — |

## FAZ 2 — Bilgi mimarisi

| Task | Tarih | Özet | build/tsc | Not |
|---|---|---|---|---|
| TASK-016 | 2026-09-01 | Marka bilgisi tek kaynak. `site-config.ts`: `BRAND` objesi (name, shortName, tagline, description, email, phone, address, instagram, currency:'TRY', locale:'tr-TR') — yer tutucular yorumlu. `SITE_DESCRIPTION` gerçekçi metinle güncellendi. `README.md` create-next-app boilerplate → projeye özel (çalıştırma, yapı, dokümanlar). `.env.example` sadeleştirildi, `example.com` → gerçekçi yer tutucu + net uyarı. | ✅ / — | tsc temiz. |
| TASK-017 | 2026-09-01 | Navigasyon tek kaynak. `lib/nav.ts`: `NavItem` tipi + `NAV_ITEMS` (Menü, Kahveni Oluştur[auth], Kahve Arenası, Hakkımızda, Giriş[!auth], Kayıt[!auth], Profil[auth]) + `visibleNavItems(isLoggedIn)`. `HeaderNav.tsx` yeniden yazıldı: hem metin nav hem ikon rayı aynı diziden, aynı sıra. Eski `navItems`/`iconItems` iki ayrı dizi kaldırıldı. `<nav aria-label>`, `<aside aria-label>` eklendi. | ✅ / ✅ | Browser: nav ve ikon linkleri birebir aynı (Menü, Kahve Arenası, Hakkımızda, Giriş Yap, Kayıt Ol — çıkışken). |
| TASK-018 | 2026-09-01 | `app/not-found.tsx` + `app/loading.tsx` + `app/error.tsx` oluşturuldu. Markalı, sade, developer dili yok. 404: "Bu sayfa bulunamadı" + Ana sayfa/Menü. loading: nötr spinner + "Yükleniyor…" (`role=status`, reduced-motion). error: "Bir şeyler ters gitti" + "Tekrar dene" (reset) + `console.error`. `styles/layout.css`'e `.route-state*` sınıfları (token bağlama TASK-028+). | ✅ / ✅ | Browser: `/bu-sayfa-yok` → özel 404. |
| TASK-019 | 2026-09-01 | Ana sayfa mimarisi + route düzeni. `app/page.tsx` `redirect("/kahvearenasii")` kaldırıldı → `<Home/>` (`app/_home/Home.tsx` iskelet + `styles/home.css`). Arena `app/kahvearenasii/` → `app/topluluk/`, `KahveArenasiClient` → `ToplulukClient`. Eski `/kahvearenasii` → `redirect("/topluluk")` stub (geri uyum). Linkler güncellendi: `lib/nav.ts`, `MenuClient` "Detaylara Git", `sitemap.ts` (`/` + `/topluluk`, priority). Metadata canonical `/topluluk`. | ✅ / ✅ | Browser: `/` gerçek ana sayfa (hero + 2 CTA); `/kahvearenasii` → `/topluluk` yönleniyor; 16 route build. |
| TASK-020 | 2026-09-01 | Ana sayfa içeriği. Bölümler: hero (marka + 2 net CTA), "Öne çıkanlar" (4 ürün kartı, `next/image`, `fmtTRY` fiyat, → `/menu`), split (Kendi kahveni tasarla → builder / Topluluk seçkisi → `/topluluk`), güven şeridi (teslimat / taze kavurma / `BRAND.email`, lucide ikon). Dev tipografi/gradient/glow yok. `home.css` genişletildi (responsive 768). `FEATURED` geçici — TASK-107'de `lib/products`'a taşınacak. | ✅ / ✅ | Browser: 4 sekme yapı doğrulandı, tüm ürün görselleri yükleniyor (₺110/₺95…), CTA'lar doğru route. |
| TASK-021 | 2026-09-01 | Footer → `components/SiteFooter.tsx`. Tüm inline `style={{}}` kaldırıldı; `styles/layout.css` `.footer*` sınıfları. `<h2>ELMENES COFFEE</h2>` → `<p class="footer-brand">` (her sayfaya h2 enjekte etme sorunu bitti). İsim/tagline/e-posta/adres `BRAND`'den. Emoji `📧 📍` → lucide `Mail`/`MapPin`. Linkler `FOOTER_LINKS` (Menü, Kahveni Oluştur, Kahve Arenası→/topluluk, Hakkımızda). Copyright yılı dinamik. | ✅ / ✅ | Browser (/menu): footer'da heading yok, inline style yok, bilgiler `BRAND`'den, "info@elmenes.com" → "merhaba@elmenescoffee.com". |
