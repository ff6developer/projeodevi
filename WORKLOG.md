# WORKLOG — Elmenes Coffee Profesyonelleştirme

Branch: `refactor/professional-overhaul` · Plan: `MASTER_TASK_PLAN.md`

Format: `TASK-XXX | tarih | özet | build / tsc | regresyon notu`

---

## FAZ 0 — Analiz & Plan

| Task | Tarih | Özet | build/tsc | Not |
|---|---|---|---|---|
| TASK-000 | 2026-09-01 | Repo tam analizi; audit ile karşılaştırma; ek bulgular E1–E20; art direction; 151 task + 7 backlog; bağımlılık sıralı plan. | ✅ / ✅ (baseline) | Baseline build: Next 16.2.0 Turbopack, TS temiz, 15 route static. |

## FAZ 1 — Kritik UX & kırık akışlar

| Task | Tarih | Özet | build/tsc | Not |
|---|---|---|---|---|
| TASK-001 | 2026-09-01 | Branch açıldı; baseline build doğrulandı; `WORKLOG.md` + `docs/CONVENTIONS.md` oluşturuldu. | ✅ / ✅ | Kod davranışı değişmedi. |
| TASK-002 | 2026-09-01 | Builder scroll-hijack kaldırıldı: `CoffeeRight.tsx` wheel `useEffect` + unused import silindi. `.coffee-right` iç scroll konteyneri (`height:100vh; overflow-y:auto`, gizli scrollbar) kaldırıldı; `.config-section` `min-height:100vh` → `padding:48px 0`. | ✅ / ✅ | Bölümler artık sayfa akışında. Sol panel `fixed` çocukları TASK-003'te düzelecek (geçici görsel bozukluk beklenir). |
| TASK-003 | 2026-09-01 | Builder sol paneli fixed/vh → normal akış. `.lab-badge/.hero-title/.hero-sub/.arena-stats-container/.coffee-name-input-wrapper/.hero-btn/.arena-btn` `position:fixed`+`*vh` kaldırıldı. `.hero-title` `82px`→`clamp(2rem,4vw,2.75rem)`. `.coffee-left` `gap:20px` + `align-self:flex-start` (stretch→içerik boyu). 1024 media query'deki anlamsız `left/bottom` override'ları temizlendi. | ✅ / ✅ | Browser (1440px): sol panel akıyor, çakışma yok, 8 config bölümü render, sayfa normal scroll. `.coffee-left` 589px, `.coffee-right` 4262px. Bölüm içi gevşek dikey boşluk TASK-088/089'da sıkılaşacak. |
| TASK-004 | 2026-09-01 | Builder CTA her zaman görünür + `disabled={!allSelected}` + ilerleme göstergesi. `SELECTION_STEPS` tek kaynak; `selectedCount`/`missingSteps`. "N / 8 seçim tamamlandı" + track/fill bar + "Kalan: …" eksik-adım anchor listesi. `ConfigSection` `<section id="section-{field}">` + `scroll-margin-top:110px`. `.arena-btn:disabled` stili. | ✅ / ✅ | Browser: 0/8 → 1/8 güncelleniyor, fill %12.5, eksik liste güncelleniyor, buton disabled kalıyor, section id'leri anchor için hazır. |
| TASK-005 | 2026-09-01 | Builder seçenek dizileri modül seviyesine (`MILK_OPTIONS`…). `DEFAULT_FORM`: opt-out'lar (Köpük Yok, Şurup/Baharat/Tatlandırıcı İstemiyorum, Değişiklik Yok) + Laktozlu Süt + Brezilya Çekirdeği + Karton Bardak seçili gelir. `ConfigSection` 24px kırmızı `*` + "(Bu kısım zorunludur)" kaldırıldı; `.required-star`/`.required-text` CSS silindi. | ✅ / ✅ | Browser: açılışta 8/8, geçerli reçete, Toplam 130 TL→₺, CTA aktif. Arena kopyalanan tarif yolu `DEFAULT_FORM` ile korunuyor. |
| TASK-006 | 2026-09-01 | Builder şeffaf fiyat dökümü: "Temel kahve ₺100" + ücretli eklentiler satır satır ("Süt Tipi · Laktozlu Süt + ₺10") + varsa "Arena indirimi (%15) − ₺X" + "Toplam". `fmtTRY` geçici `Intl.NumberFormat('tr-TR', TRY)` (TASK-074'te lib'e taşınacak). Yaratıcılık puanı kutusuna 1 cümlelik açıklama. Eski tek satır "{total} TL" kaldırıldı. | ✅ / ✅ | Browser: seçim değişince döküm + toplam + skor anında güncelleniyor (Laktozsuz Süt → +₺15, Toplam ₺135). |
| TASK-007 | 2026-09-01 | Sipariş durum modeli + tipler. `lib/types.ts`: `RecipeOption/CoffeeRecipe/Product/CartLine/Address/DeliveryMethod/OrderStatus/OrderItem/Order/OrderInput`. `lib/orders.ts`: `getOrders/getOrder/createOrder/updateOrderStatus/deleteOrder`, `nextStatus`, `STATUS_FLOW`, `STATUS_LABEL`, `legacyStatusToCanonical`, `parseOrderDate`, `migrateLegacy` (eski `"orders"` → kanonik `elmenes.orders`). SSR guard'lı. | ✅ / — | Henüz hiçbir yerden import edilmiyor; wiring TASK-008/011/122. tsc temiz. |
| TASK-008 | 2026-09-01 | `/siparis` yeniden yazıldı: sonsuz "Hazırlanıyor…" + `setInterval` `dots` + `.siparis-overlay`/blur/keyframe animasyonlar kaldırıldı. Yeni: yeşil onay ikonu + "Siparişin alındı" + "Sipariş no: #A1042" + kalem kalem özet (kalem(ler), ara toplam, indirim, teslimat, toplam) + durum rozeti + ETA metni. `getOrder(?o=)` / son sipariş `lib/orders`. Boş durum: "Görüntülenecek sipariş bulunamadı" + Menüye git. `page.tsx` `<Suspense>` (useSearchParams). `siparis.css` sadeleştirildi (fixed bg → normal akış, tam refactor TASK-090). JSX girintisi düzeltildi. | ✅ / ✅ | Browser: hem dolu (fixture A1042: ₺145 toplam, indirim ₺5) hem boş durum doğrulandı. |
| TASK-009 | 2026-09-01 | Kullanıcıya görünen developer/hata mesajları insancıllaştırıldı. `AuthForm.tsx`: "Server'a ulaşılamıyor. Lütfen backend terminalini kontrol edin!" → "Şu an işlem tamamlanamadı. Lütfen birazdan tekrar dene." · "Bir hata oluştu" → "Bir şeyler ters gitti, tekrar dener misin?" (`RegisterClient` de). `console.error` mesajları korundu (kullanıcı görmez). Grep: JSX/toast metninde "backend/server/terminal" kalmadı. | ✅ / — | tsc temiz; build TASK-010 ile. |
| TASK-010 | 2026-09-01 | Builder "Laboratuvarı Aç" yapay ön adımı kaldırıldı. `started` state ve `setStarted` tamamen silindi; içerik ilk render'da görünür. `CoffeeRight` `started` prop'u kaldırıldı, config bölümleri hep render. `.hero-btn` CSS + kullanılmayan `@keyframes readyPop` + artık no-op olan 768 media query bloğu temizlendi. Locked-banner'daki `🔒` emoji kaldırıldı (lucide Lock zaten var). | ✅ / ✅ | Browser: `/kahveniolustur` açılışta tam builder (stats + fiyat + isim + CTA + 8 bölüm) — ekstra tık yok. Arena kilitli tarif yolu korunuyor. |
| TASK-011 | 2026-09-01 | Builder → sipariş `lib/orders.createOrder()` üzerinden (kanonik `elmenes.orders`, kuruş cinsinden tutarlar, id `A####`). Redirect `/siparis?o=<id>`. Geçiş dönemi: Admin hâlâ eski `"orders"` okuduğu için aynı id ile eski formatta bir kopya da yazılıyor (TASK-122'de kaldırılacak). `coffees` yazımı korundu (profil/arena). Toast metinleri sadeleşti. | ✅ / ✅ | Browser uçtan uca: builder → sipariş → `/siparis?o=A4708`; kanonik kayıt + legacy mirror (aynı id) doğrulandı. Davranış notu: eski dağınık elle yazım → tek veri katmanı; admin görünürlüğü TASK-122'de tamamlanacak. |
| TASK-012 | 2026-09-01 | Arena feed/podium boş durumu. `!isLoading && posts.length===0` → `.arena-empty` bloğu (Coffee ikonu + "Henüz tasarım paylaşılmamış" + açıklama + "Kahveni tasarla" → `/kahveniolustur`). Skeleton mantığı korundu. `.arena-empty` CSS eklendi (token: `--primary-coffee/--primary-hover`). | ✅ / ✅ | Browser: `arenaPosts` temiz → boş durum render, grid render edilmiyor. |
| TASK-013 | 2026-09-01 | Arena sahte sosyal kanıt + tutarsız indirim düzeltildi. `+124` + 3 sabit avatar (`/avatar1-3.jpg`) kaldırıldı → gerçek `participantCount` (benzersiz `userName`), sadece >0 ise "N kişi bu ay tasarım paylaştı". Reward "%20 İndirim Kuponu" → "%15" (builder ile tutarlı). Drawer "Master Barista" sabiti → `arenaScore>=50 ? "Usta Barista" : "Barista"`. `.join-avatars/.join-avatar` CSS silindi, `.join-count` metin stiline döndü. Başlık "SEN DE ARENADA YERİNİ AL!" → "Sen de yerini al". | ✅ / ✅ | tsc + build yeşil. |
| TASK-014 | 2026-09-01 | Arena yanıltıcı ikon düzeltildi. `.copy-recipe-btn` `ShoppingCart` → `ClipboardCopy` (lucide). `aria-label`: "Tarifi Kopyala" → "Tarifi kahve tasarımına kopyala". Toast: "Tarif laboratuvara aktarıldı! %15 indirim kazandın!" → "Tarif kahve tasarımına aktarıldı — %15 indirim uygulandı." | ✅ / — | tsc temiz; build TASK-015 checkpoint'te. |
| TASK-015 | 2026-09-01 | **FAZ 1 REGRESSION CHECKPOINT.** `npx next build` ✅ (15 route static). `npx tsc --noEmit` ✅. `npx eslint app/siparis` temiz; dokunulan dosyalarda yeni `any` yok (lib/orders `unknown`'a çevrildi). Kritik akışlar: builder render + varsayılan reçete + fiyat dökümü + ilerleme + CTA ✅; builder→`createOrder`→`/siparis?o=` onay ✅; `/siparis` dolu+boş ✅; arena boş+dolu, sahte veri yok ✅; menü/profil çökmüyor ✅; auth hata mesajları insancıl ✅. Not: `next dev` konsolunda TASK-010/013 ara-kayıt anlarından kalma bayat HMR parse hataları görünüyor; `next build` + `tsc` + canlı DOM temiz — bayat buffer. Ön mevcut `no-explicit-any` lint hataları (dokunulmamış dosyalar) → TASK-148. **FAZ 1 COMPLETE.** | ✅ / ✅ | — |

## FAZ 2 — Bilgi mimarisi

| Task | Tarih | Özet | build/tsc | Not |
|---|---|---|---|---|
| TASK-016 | 2026-09-01 | Marka bilgisi tek kaynak. `site-config.ts`: `BRAND` objesi (name, shortName, tagline, description, email, phone, address, instagram, currency:'TRY', locale:'tr-TR') — yer tutucular yorumlu. `SITE_DESCRIPTION` gerçekçi metinle güncellendi. `README.md` create-next-app boilerplate → projeye özel (çalıştırma, yapı, dokümanlar). `.env.example` sadeleştirildi, `example.com` → gerçekçi yer tutucu + net uyarı. | ✅ / — | tsc temiz. |
| TASK-017 | 2026-09-01 | Navigasyon tek kaynak. `lib/nav.ts`: `NavItem` tipi + `NAV_ITEMS` (Menü, Kahveni Oluştur[auth], Kahve Arenası, Hakkımızda, Giriş[!auth], Kayıt[!auth], Profil[auth]) + `visibleNavItems(isLoggedIn)`. `HeaderNav.tsx` yeniden yazıldı: hem metin nav hem ikon rayı aynı diziden, aynı sıra. Eski `navItems`/`iconItems` iki ayrı dizi kaldırıldı. `<nav aria-label>`, `<aside aria-label>` eklendi. | ✅ / ✅ | Browser: nav ve ikon linkleri birebir aynı (Menü, Kahve Arenası, Hakkımızda, Giriş Yap, Kayıt Ol — çıkışken). |
| TASK-018 | 2026-09-01 | `app/not-found.tsx` + `app/loading.tsx` + `app/error.tsx` oluşturuldu. Markalı, sade, developer dili yok. 404: "Bu sayfa bulunamadı" + Ana sayfa/Menü. loading: nötr spinner + "Yükleniyor…" (`role=status`, reduced-motion). error: "Bir şeyler ters gitti" + "Tekrar dene" (reset) + `console.error`. `styles/layout.css`'e `.route-state*` sınıfları (token bağlama TASK-028+). | ✅ / ✅ | Browser: `/bu-sayfa-yok` → özel 404. |
| TASK-019 | 2026-09-01 | Ana sayfa mimarisi + route düzeni. `app/page.tsx` `redirect("/kahvearenasii")` kaldırıldı → `<Home/>` (`app/_home/Home.tsx` iskelet + `styles/home.css`). Arena `app/kahvearenasii/` → `app/topluluk/`, `KahveArenasiClient` → `ToplulukClient`. Eski `/kahvearenasii` → `redirect("/topluluk")` stub (geri uyum). Linkler güncellendi: `lib/nav.ts`, `MenuClient` "Detaylara Git", `sitemap.ts` (`/` + `/topluluk`, priority). Metadata canonical `/topluluk`. | ✅ / ✅ | Browser: `/` gerçek ana sayfa (hero + 2 CTA); `/kahvearenasii` → `/topluluk` yönleniyor; 16 route build. |
| TASK-020 | 2026-09-01 | Ana sayfa içeriği. Bölümler: hero (marka + 2 net CTA), "Öne çıkanlar" (4 ürün kartı, `next/image`, `fmtTRY` fiyat, → `/menu`), split (Kendi kahveni tasarla → builder / Topluluk seçkisi → `/topluluk`), güven şeridi (teslimat / taze kavurma / `BRAND.email`, lucide ikon). Dev tipografi/gradient/glow yok. `home.css` genişletildi (responsive 768). `FEATURED` geçici — TASK-107'de `lib/products`'a taşınacak. | ✅ / ✅ | Browser: 4 sekme yapı doğrulandı, tüm ürün görselleri yükleniyor (₺110/₺95…), CTA'lar doğru route. |
| TASK-021 | 2026-09-01 | Footer → `components/SiteFooter.tsx`. Tüm inline `style={{}}` kaldırıldı; `styles/layout.css` `.footer*` sınıfları. `<h2>ELMENES COFFEE</h2>` → `<p class="footer-brand">` (her sayfaya h2 enjekte etme sorunu bitti). İsim/tagline/e-posta/adres `BRAND`'den. Emoji `📧 📍` → lucide `Mail`/`MapPin`. Linkler `FOOTER_LINKS` (Menü, Kahveni Oluştur, Kahve Arenası→/topluluk, Hakkımızda). Copyright yılı dinamik. | ✅ / ✅ | Browser (/menu): footer'da heading yok, inline style yok, bilgiler `BRAND`'den, "info@elmenes.com" → "merhaba@elmenescoffee.com". |
| TASK-022 | 2026-09-01 | Metadata denetimi. `layout.tsx` metadata zaten `site-config`'ten besleniyor (metadataBase, title template `%s | Elmenes Coffee`, OG/twitter). Ana sayfa `page.tsx` metadata TASK-019/020'de eklendi (title absolute, canonical `/`). Menü/topluluk metadata mevcut. Bu task'ta: OG görseli için "1200×630 yer tutucu" notu + `themeColor` için "TASK-028 token" notu eklendi. | ✅ / — | Fonksiyonel değişiklik minimal; metadata sağlıklı. |
| TASK-023 | 2026-09-01 | `@/` import alias tutarlılığı. Tüm cross-dir importlar (`../components`, `../../lib`, `../../styles`, `../site-config`, `../app/site-config`) → `@/...`. `components/OrdersPanel` `../components/OrderCard` → `./OrderCard`. Aynı-klasör importlar relatif kaldı. Sadece import satırları; mantık değişmedi. | ✅ / ✅ | `grep "../.." app components` boş. tsc + build yeşil. |
| TASK-024 | 2026-09-01 | Arena → "Topluluk" konumlandırma metinleri (sadece metin, işlev korundu). `h1` "KAHVE ARENASI" → "Topluluk"; alt başlık "Şampiyonlar Ligine Katıl!" → "Kendi kahveni tasarla, toplulukla paylaş, oy ver."; days-badge "Bu ayın seçkisi — N gün kaldı"; "TURNUVA SÜRECİ" → "Nasıl çalışır"; step "Şampiyon Ol" → "Öne çık", "Ödül Kazan" desc yumuşatıldı; podium "🏆 Anlık Sıralama" → "Bu ayın sıralaması", "Şu an lider" → "Şu an önde"; reward descs "Kazanan/şampiyon" → "Ayın öne çıkanı". `lib/nav` + `SiteFooter` label "Kahve Arenası" → "Topluluk"; `topluluk/page.tsx` title "Topluluk"; MenuClient "🏅 KAHVE ARENASI ŞAMPİYONU" → "Ayın öne çıkan tasarımı". | ✅ / ✅ | Oy/yorum/sıralama/kopyala işlevleri değişmedi. |
| TASK-025 | 2026-09-01 | Menü ürün adları düzeltildi / tek dil & Title Case. Cappicino→Cappuccino, Filtre→Filtre Kahve, Macciato→Macchiato, "Türk kahvesi"→"Türk Kahvesi", "Iced caramel Macchiato"→"Iced Caramel Macchiato", "ispanyol creamy"→"İspanyol Usulü Cheesecake", "frambuazlı Cheesecake"→"Frambuazlı Cheesecake", cookie→Cookie, "Çikolatalı donat"→"Çikolatalı Donat". Görsel dosya adları aynı (public). | ✅ / ✅ | Sadece görünen ad; görseller doğru eşleşiyor. |
| TASK-026 | 2026-09-01 | Turnuva sayacı takvim ayına bağlandı. `lib/community.ts`: `getCurrentPeriodKey()` ("2026-09"), `getRemainingDays()` (ay sonuna kadar), `rollOverIfNeeded(posts)` (dönem değişince önceki ayın en çok oy alanını `arenaChampion`'a arşivle + seçkiyi sıfırla). `ToplulukClient` iki `useEffect` sadeleşti; `tournamentStart`/`checkTournament` (`Date.now()` tabanlı, cihaz bazlı) kaldırıldı → `arenaPeriod`. | ✅ / ✅ | Browser: badge "Bu ayın seçkisi — 30 gün kaldı" (01.09 → ay sonu), `arenaPeriod=2026-09`. Cihazdan bağımsız. |
| TASK-027 | 2026-09-01 | **FAZ 2 REGRESSION CHECKPOINT.** `next build` ✅ (16 route). `tsc` ✅. Route smoke: `/`→200 (gerçek ana sayfa), tüm ana route'lar 200, `/kahvearenasii`→redirect→`/topluluk`, `/bu-yok`→404 (özel). Footer `layout.tsx`'te → her sayfada. 404/loading/error dosyaları yerinde. Nav tek kaynak, metin+ikon aynı. **FAZ 2 COMPLETE.** | ✅ / ✅ | — |

## FAZ 3 — Design system

| Task | Tarih | Özet | build/tsc | Not |
|---|---|---|---|---|
| TASK-028 | 2026-09-01 | `styles/tokens.css` — tek token kaynağı. **Dark-first**: bare `:root` = koyu palet (mevcut site yönüyle uyumlu, ara fazlarda tema karışmasın diye); açık tema `@media(prefers-color-scheme:light) :root:not([data-theme="dark"])` + `:root[data-theme="light"]`; `:root[data-theme="dark"]` açıkça. Token grupları: yüzey/mürekkep (paper/surface/ink 1-3/line), aksan (tek "roast" rengi + contrast), semantik (success/warning/danger/info), elevation (`--shadow-1/2`, temaya göre), tipografi (font aileleri + 7 kademe fs + lh + tracking), spacing `--s-1..9` (4px), radius `--r-sm/md/lg/pill`, z-index, motion (`--t-fast/base`, `--ease`), layout (`--container`, `--header-h`, `--rail-gap`). `layout.tsx`'te ilk import. | ✅ / ✅ | Henüz hiçbir bileşen token kullanmıyor (bağlama TASK-030+); mevcut sayfalar etkilenmiyor. Font aileleri geçici olarak var(--font-inter/playfair)'e map'li (TASK-043 Fraunces+Plex). |
| TASK-029 | 2026-09-01 | `app/globals.css` silindi (hiçbir yerden import edilmiyordu; ölü `:root` + `.glass-card`). `grep "globals.css\|glass-card"` sadece dosyanın kendisiydi. | ✅ / ✅ | Build etkilenmedi. |
| TASK-030 | 2026-09-01 | `styles/base.css` — tek reset + taban element stilleri. Modern reset, `body` token'lardan (`--paper`/`--ink`/`--font-sans`/`--fs-body`/`--lh-body`), `h1-h4` (`--font-display` + fs kademeleri + `text-wrap:balance`), `p`/`a`/`ul`/`hr`, global `:focus-visible` halkası, `:focus:not(:focus-visible)` sıfırla, `::selection`, global `@media(prefers-reduced-motion:reduce)`. `layout.css`'ten reset + `html,body` bloğu + `.layout` radial-gradient (glow) çıkarıldı. `overflow-x:hidden` battaniyesi kaldırıldı (gerçek taşmalar TASK-131). `layout.tsx` import sırası: tokens → base → layout. `error.tsx` `<a>` → `<Link>` (lint). | ✅ / ✅ | curl: `/`,`/menu`,`/topluluk` 200. Browser: topluluk render tamam, kırılma yok. Not: bazı sayfa-özel CSS'ler henüz token kullanmıyor (TASK-034+). |
| TASK-031 | 2026-09-01 | Kullanılmayan Tailwind kaldırıldı. `grep "@tailwind\|@apply\|tailwindcss"` styles/app → boş. `postcss.config.mjs` silindi (tek içeriği `@tailwindcss/postcss` idi). `package.json` devDeps'ten `tailwindcss` + `@tailwindcss/postcss` çıkarıldı; `npm install` (13 paket kaldırıldı). | ✅ / ✅ | build yeşil; Turbopack postcss config'siz sorunsuz. |
| TASK-032 | 2026-09-01 | Sınıf çakışmaları çözüldü. `.arena-btn` (menu.css + kahveniolustur.css, çelişkili tanım): kahveniolustur → `.builder-submit-btn`, menu → `.menu-champion-btn` (CSS + TSX). `.product-card` (adminpanel + menu): adminpanel → `.admin-product-card` (`ProductsPanel.tsx` + CSS). `.score-badge` (adminpanel + kahvearenasi): admin `OrderCard` → `.admin-score-badge` (+ CSS). Diğer şüpheli sınıflar (`comment-btn`, `empty-state`, `stat-item`) tek dosyada — çakışma değil. | ✅ / ✅ | `grep` ile 3 sınıf artık tek dosyada. Kalıcı çözüm (CSS Modules) TASK-104. |
| TASK-033 | 2026-09-01 | Renk token migrasyonu — `layout.css` + `login.css`. `login.css` `:root` bloğu silindi, tamamen token'a çevrildi (yüzey/mürekkep/aksan/radius/spacing/motion); `blur-active::before` + kart glow + buton gradient + input glow kaldırıldı, sade token stilleri. `layout.css`: `#c58a5c`→`--accent`, `#ffffff`→`--ink`, `#b3b3b3`→`--ink-2`, `#8a8279`→`--ink-3`, `rgba(255,255,255,.08)`→`--line`, `#050505`→`--surface`. Kalan efektler (`.logo-text` gradient, `.iconbar` blur, toast blur, scrollbar) sonraki tasklarda (TASK-045/058/067/142). | ✅ / ✅ | build yeşil. |
| TASK-034 | 2026-09-01 | `kahvearenasi.css` renk token migrasyonu. Renk `:root` bloğu silindi; `var(--primary-coffee/--primary-hover/--arena-gold/--arena-silver/--arena-bronze/--dark-bg/--card-bg/--glass-border/--transition-smooth)` → token karşılıkları (`--accent`, `--accent-weak`, `--ink-2`, `--paper`, `--surface`, `--line`, `all var(--t-base) var(--ease)`). Ham hex (`#c58a5c/#ffffff/#050505/#1a1a1a/#888/#aaa/#ccc/#666`) → token. Arena "gold" kimliği `--accent`'e indi. Layout `--arena-padding-*` `:root`'u (clamp) korundu — TASK-091'de ele alınacak. Efektler (noise bg, glow, gradient text, 3D) TASK-091/142. | ✅ / ✅ | build yeşil. |
| TASK-035 | 2026-09-01 | `menu.css` — açık tema adası (TASK-085 ana temaya çekecek). Bu turda ağır renk migrasyonu yapılmadı (dark token'a bağlamak açık kartları bozardı). `--font-inter` referansı `--font-sans`'a TASK-043'te geçecek. Not düşüldü. | ✅ / ✅ | — |
| TASK-036 | 2026-09-01 | `kahveniolustur.css` renk token migrasyonu (node script). Ham hex → token; `:root` yoktu. Efektler (coffee-bg, blur option-group, milk-item translateX/glow, submit gradient) TASK-088/089/142. | ✅ / ✅ | build yeşil. |
| TASK-037 | 2026-09-01 | `profil.css` renk token migrasyonu. `:root` (`--primary-coffee/--primary-hover/--arena-gold/--card-bg/--border-light/--text-muted/--transition`) silindi → token; ham hex → token. Efekt/layout refactor TASK-094-096. | ✅ / ✅ | build yeşil. |
| TASK-038 | 2026-09-01 | `siparis.css` renk token migrasyonu (TASK-008'de yazılan hardcoded değerler token'a). | ✅ / ✅ | build yeşil. |
| TASK-039 | 2026-09-01 | `adminpanel.css` renk token migrasyonu. 30+ satırlık ayrı token `:root`'u (`--bg-*`, `--accent-gold`, `--text-*`, `--radius-sm/md/lg/full`, `--shadow-*`, `--border-*`) silindi → tokens.css karşılıkları (`--surface*`, `--accent`, `--ink*`, `--r-*`, `--shadow-1/2`, `--line*`). Gold sistemi `--accent`'e indi. `--success/--danger` tokens.css'ten geliyor. `--status-color/--status-glow` runtime (Dashboard inline) korundu. | ✅ / ✅ | build yeşil. |
| TASK-040 | 2026-09-01 | `hakkimizda.css` renk + ölçek migrasyonu. `:root` (`--spacing-xs..lg` + `--font-xs..hero` clamp ölçeği) silindi → `--s-*` / `--fs-*` token'ları. Ham hex (brown/gri) → token. İçerik/layout refactor TASK-099. | ✅ / ✅ | build yeşil. |
| TASK-041 | 2026-09-01 | Radius & shadow global token sweep (node script, 9 CSS dosyası). Tüm `border-radius: Npx` → en yakın `var(--r-sm/md/lg/pill)`; asimetrik (üst köşe / 4-değer) → token'lı korundu; `50%` dokunulmadı. Saf glow `box-shadow: 0 0 …` ve `inset 0 0 …` deklarasyonları tamamen silindi; kalan `box-shadow` → `var(--shadow-1)`. ~135 radius + ~63 shadow değişimi. | ✅ / ✅ | build yeşil. Aykırı eşleşmeler + `--shadow-2` ayrımı TASK-143'te. |
| TASK-042 | 2026-09-01 | **FAZ 3 REGRESSION CHECKPOINT.** `next build` ✅ (16 route), `tsc` ✅. 9 route smoke → hepsi 200. Token'lar canlı: `body` bg = `--paper` (#17140f), `--accent` #d98a5c, `--r-md` 12px. Browser: `/giris` tamamen token-tabanlı, glassmorphism/blur/glow gitti, tutarlı; `/` ana sayfa serif başlık + warm-dark zemin. Sayfa CSS'lerinde renk `:root`'u kalmadı (yalnız `kahvearenasi.css`'te layout clamp `:root`'u — TASK-091). Tek token kaynağı: `tokens.css`. `globals.css` yok. Tailwind yok. **FAZ 3 COMPLETE.** | ✅ / ✅ | — |

## FAZ 4 — Typography & spacing

| Task | Tarih | Özet | build/tsc | Not |
|---|---|---|---|---|
| TASK-043 | 2026-09-01 | Fontlar değişti. `layout.tsx`: `Inter` + `Playfair_Display` kaldırıldı → **`Fraunces`** (display, w400/500/600), **`IBM_Plex_Sans`** (sans, w400/500/600), **`IBM_Plex_Mono`** (mono, w400/500). Tümü `latin-ext` (Türkçe). `<html>` className `--font-fraunces --font-plex-sans --font-plex-mono`. `tokens.css` `--font-display/--font-sans/--font-mono` yeni değişkenlere. Tüm CSS'lerde `var(--font-inter)` → `var(--font-sans)`, `var(--font-playfair)` → `var(--font-display)`; `'Inter'`/`'Playfair Display'` fallback'leri temizlendi. | ✅ / ✅ | Browser: ana sayfa h1 Fraunces serif, gövde Plex Sans, Türkçe karakterler doğru. |
| TASK-044 | 2026-09-01 | `styles/typography.css` — yardımcı sınıflar: `.text-lg/.text-sm/.text-caption`, `.eyebrow` (tek uppercase kullanımı — küçük etiket), `.text-mono` (tabular-nums), `.text-muted`, `.text-balance`. Element defaultları zaten `base.css`'te (h1-h4 Fraunces + fs kademeleri + `text-wrap:balance`). `layout.tsx` import sırasına eklendi (base'den sonra). | ✅ / ✅ | build yeşil. Uygulama TASK-046+/Faz 7. |
| TASK-045 | 2026-09-01 | Gradient text kaldırıldı. `.logo-text` (layout.css): gradient + `background-clip:text` + `color:transparent` → düz `var(--ink)`, `font-weight:600`, `letter-spacing:.02em`, uppercase korundu (logo). `.arena-title-area h1`: `950` weight + `letter-spacing:10px` + gradient + `drop-shadow` → `var(--fs-h1)` / `600` / düz `var(--ink)`. `.profil-name`: `900`/`-2px`/gradient → `var(--fs-h2)` / `600` / `var(--ink)`. `grep text-fill-color/background-clip` → boş. | ✅ / ✅ | build yeşil. |
| TASK-046 | 2026-09-01 | Aşırı uppercase / letter-spacing / font-weight temizliği (node sweep, 9 CSS). Tüm `text-transform:uppercase` kaldırıldı (13 adet) — `.eyebrow` yardımcı sınıfı `typography.css`'te kalıyor. Tüm `letter-spacing: Npx` → `normal` (34 adet). `font-weight: 700/800/900/950/bold` → `600` (73 adet). | ✅ / ✅ | Browser: topluluk h1 artık Fraunces/600/none/normal (eski 950 + 10px + uppercase gitti). home eyebrow TASK-084'te `.eyebrow` sınıfına geçecek. |
| TASK-047 | 2026-09-01 | Spacing token migrasyonu — `layout.css`, `home.css`, `login.css`, `siparis.css` (birleşik node sweep'in parçası). `gap:Npx` + 1/2-değerli `padding`/`margin:Npx` → en yakın `var(--s-1..9)` (4px tabanlı ölçek). >96px değerler korundu (sayfa-seviye, Faz 7/9). | ✅ / ✅ | build yeşil; login görsel doğrulandı. |
| TASK-048 | 2026-09-01 | Spacing token migrasyonu — `menu.css` + `kahvearenasi.css`. (aynı sweep) | ✅ / ✅ | build yeşil. |
| TASK-049 | 2026-09-01 | Spacing token migrasyonu — `kahveniolustur.css` + `profil.css`. (aynı sweep) | ✅ / ✅ | build yeşil. |
| TASK-050 | 2026-09-01 | Spacing token migrasyonu — `adminpanel.css`. (aynı sweep) | ✅ / ✅ | build yeşil. |
| TASK-051 | 2026-09-01 | Spacing token migrasyonu — `hakkimizda.css` (kendi `--spacing-*` seti TASK-040'ta zaten `--s-*`'e geçmişti; kalan çıplak px → token). Toplam sweep: 290 değişim. | ✅ / ✅ | build yeşil. |
| TASK-052 | 2026-09-01 | **FAZ 4 REGRESSION CHECKPOINT.** `next build` ✅, `tsc` ✅. Fraunces (display) + IBM Plex Sans (metin) + Plex Mono (veri) canlı; h1-h4 `base.css`'te ölçek + Fraunces. `text-transform:uppercase` yalnız `typography.css .eyebrow`'da. Gradient text = 0. Font-weight 700-950 → 600. Spacing token'lı (290 değişim). Browser: `/giris`, `/topluluk` tutarlı hiyerarşi; `/menu` hâlâ açık tema adası (TASK-085). **FAZ 4 COMPLETE.** | ✅ / ✅ | — |

## FAZ 5 — Navigation

| Task | Tarih | Özet | build/tsc | Not |
|---|---|---|---|---|
| TASK-053 | 2026-09-01 | `HeaderNav` yeniden yazıldı: fixed glass pill + `position:fixed` logo → **düz sticky header** (`--surface` + alt `--line` border, `--header-h:64px`). Sol: logo + "Elmenes Coffee" wordmark (Fraunces). Sağ: yatay nav (`>=lg`). `NAV_ITEMS` tek kaynak (TASK-017). | ✅ / ✅ | Browser: temiz düz header, glass/pill gitti. |
| TASK-054 | 2026-09-01 | Tablet & mobil: `< lg` (1024) hamburger → `components/NavDrawer.tsx` (erişilebilir: `role=dialog aria-modal`, ESC, focus-trap, `body` scroll-lock, `aria-expanded/controls`, rota değişince kapanır). Sağdan panel. Alt bar YOK — drawer tek çözüm. | ✅ / ✅ | Browser 375px: hamburger → drawer 5 link, ESC kapatıyor, scroll-lock çalışıyor. |
| TASK-058 | 2026-09-01 | `iconbar` + `.icon-*` + eski `.header/.header-container/.logo/.logo-text/.nav` CSS tamamen silindi (base + tüm media query'ler). `body{padding-bottom:75px}` (eski alt bar) kaldırıldı. Sayfa üst padding'leri (140px/120px/110px/100px) → `var(--s-6/s-7)` (sticky header artık akışta). `.arena-page padding-left:110px` (ray payı) → simetrik. `.toast-viewport top` → `calc(var(--header-h) + --s-*)`. Hakkımızda artığı media kuralları korundu (TASK-099'da taşınacak). | ✅ / ✅ | build yeşil. |
| TASK-055 | 2026-09-01 | Aktif / hover / focus durumları — TASK-053 nav yeniden yapısında zaten uygulandı: `usePathname()` + `.is-active` + `aria-current="page"` (accent alt çizgi, glow yok); hover `--ink-2`→`--ink` (`--t-fast`); global `:focus-visible` halkası `base.css`'te. Drawer link'lerinde de aktif/hover/focus. | ✅ / ✅ | — |
| TASK-056 | 2026-09-01 | Mobil touch target & header yüksekliği. `--header-h: 96px` → `64px`. Drawer link `min-height:44px`, toggle 44×44, close 44×44. `.toast-viewport top` = `calc(--header-h + --s-*)`. Header artık sticky (akışta) → içerik örtme yok. | ✅ / ✅ | — |
| TASK-057 | 2026-09-01 | Kapat/geri ikon tutarlılığı — standart `NavDrawer`'da tanımlandı (lucide `X` + `aria-label="Kapat"`). Menü modal / profil modal / arena drawer kapatma butonları ilgili component migrasyonunda (Faz 6/7) bu standarda geçecek. | ⏳ kısmi | Component migrasyonuyla tamamlanacak. |
| TASK-059 | 2026-09-01 | `lib/session.ts` — `getUser/isLoggedIn/isAdmin/setUser/setAdmin/clearSession/subscribe`. `localStorage["user"]` erişimi tek yerde. Bağlandı: `HeaderNav` (`subscribe`), `LoginClient` (`setUser`), `ProfilClient` (`getUser`/`setSessionUser`/`clearSession`), `AdminPanelClient` (`isAdmin()`), `KahveniOlusturClient` (`isLoggedIn()`). `AuthForm` adminCheck generic setItem korundu (not: authChanged fire etmiyor — admin nav-bağımlı değil). | ✅ / ✅ | build+tsc yeşil. |
| TASK-060 | 2026-09-01 | Skip link + landmark. `<a href="#main" class="skip-link">İçeriğe geç</a>` (layout.tsx, HeaderNav'dan önce), `<main id="main">`. `.skip-link` sadece `:focus-visible`'da görünür (accent, token'lı). `<header>`/`<nav aria-label>`/`<footer>`/`<main>` landmark'ları yerinde. | ✅ / ✅ | build yeşil. |
