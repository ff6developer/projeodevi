# WORKLOG V2 — Production / Commercial Readiness

Ref: `MASTER_TASK_PLAN_V2.md` · `AUDIT_PRODUCTION.md`
Format: `TASK-XXX | tarih | özet | build/tsc/lint | not`

---

## FAZ A — Production blockers

| Task | Tarih | Özet | build/tsc/lint | Not |
|---|---|---|---|---|
| TASK-201 | 2026-09-03 | `backend/` (Express + commit'li `node_modules`, 595 dosya) repodan silindi; `vercel.json` (`services.backend` + `/api/backend/*` rewrite) silindi — Vercel Next'i otomatik algılar. `.gitignore`'a `**/node_modules`. `package.json` adı `proje-odev` → `elmenes-coffee`. | ✅ / ✅ | Auth hâlâ `API_BASE_URL`'e bakıyor — TASK-203/204'te kesilecek. |
| TASK-202 | 2026-09-03 | Kullanılmayan `html2canvas` bağımlılığı kaldırıldı (`grep` = 0 kullanım); `npm install` ile lock + node_modules temizlendi. Kalan deps: lucide-react, next, react, react-dom. | ✅ / ✅ | — |
| TASK-203 | 2026-09-03 | **AUTH BLOKER ÇÖZÜLDÜ.** `lib/services/` katmanı: `types.ts` (`Result<T>`, `sleep`, `MOCK_LATENCY_MS`), `adapters/local/storage.ts` (güvenli localStorage sarmalayıcı — tarayıcı depolamasına TEK erişim noktası), `auth.ts` (`AuthService` interface), `adapters/local/auth.ts` (mock impl: `elmenes.users`'a SADECE `{name,email}` — parola saklanmaz; register→auto-login, dup e-posta hatası, login kayıtlı e-posta kontrolü, `loginDemo()`). `index.ts` barrel. `fetch`/`API_BASE_URL`/`/api/backend/*` tamamen kaldırıldı. | ✅ / ✅ | Prod'da doğrulandı: kayıt→giriş→builder→checkout, hiçbir network isteği yok. |
| TASK-204 | 2026-09-03 | `AuthForm` yeniden yazıldı: `submitUrl/submitMethod/fetch/adminCheck` çıktı; `onSubmit: (data) => Promise<AuthSubmitResult>` + opsiyonel `onDemo` prop. `LoginClient`/`RegisterClient` `authService`'e bağlandı, `?next=` göreli yol korumalı. "Demo hesabıyla dene" butonu + "portföy — hesaplar bu tarayıcıda" notu. `login-divider`/`login-demo-note` CSS. `kayit/page.tsx` `Suspense`. | ✅ / ✅ | Alan bazlı hata (dup e-posta, kayıtsız e-posta) inline `role=alert` doğrulandı. |
| TASK-205 | 2026-09-03 | Gömülü `admin@gmail.com/admin123` **tamamen kaldırıldı** (`grep` = 0). `/adminpanel`: `isAdmin` değilse → dürüst demo geçidi (`ShieldCheck` + "portföy demosu, gerçek yetki backend ile" + "Demo yönetici görünümüne gir" butonu → `setAdmin(true)`). `useRouter` kaldırıldı. `.admin-gate` CSS. | ✅ / ✅ | Geçit + buton prod'da doğrulandı. |
| TASK-206 | 2026-09-03 | `SITE_URL` fallback zinciri: `NEXT_PUBLIC_SITE_URL` → Vercel `VERCEL_PROJECT_PRODUCTION_URL` / `NEXT_PUBLIC_VERCEL_*` → `localhost:3000`. `example.com` yer tutucusu kaldırıldı. `.env.example` (yeni). `BRAND.tagline` → "Taze kavrulmuş kahve, kapına gelir"; `BRAND.isDemo:true`. `API_BASE_URL` export kaldırıldı. | ✅ / ✅ | robots.txt / sitemap.xml'de `example.com` YOK (env'siz `localhost`). |
| TASK-207 | 2026-09-03 | `viewport.themeColor` `#080808` → `#17140f` (`--paper`). | ✅ / ✅ | — |
| TASK-208 | 2026-09-03 | `app/opengraph-image.tsx` (`next/og` `ImageResponse`, 1200×630, marka paleti, dinamik). `layout.tsx` OG/Twitter'dan `logo.png` referansları kaldırıldı, `card: summary_large_image`. | ✅ / ✅ | `/opengraph-image` → 200 image/png. |
| TASK-209 | 2026-09-03 | Hydration #418: FRESH prod sunucuda (temiz port, temiz localStorage + seed'li) `/`, `/menu`, `/giris`, `/kayit` — **konsol tamamen temiz, #418 YOK**. Önceki #418 + 500'ler zombie-server (port çakışması) artefaktıymış. Kalan: "preloaded but not used" CSS uyarıları (hata değil — TASK-244b). | ✅ / ✅ | Tüm 19 route taraması TASK-212'de. |
| TASK-210 | 2026-09-03 | `app/dev/` (UI component galerisi) repodan silindi — public deploy'da `/dev/*` route'u olmaz. `/dev/ui` → 404. | ✅ / ✅ | — |
| TASK-211 | 2026-09-03 | `robots.ts`: tüm özel alanlar disallow (`/adminpanel /profil /giris /kayit /sepet /odeme /siparis /siparislerim`) + `host`. `sitemap.ts`: yalnız public + değerli sayfalar, `lastModified` + `changefreq` + `priority`; ürün + yasal sayfalar sonraki tasklarda eklenecek. | ✅ / ✅ | — |
| TASK-212 | 2026-09-03 | **FAZ A regresyon checkpoint.** `next build` ✅ (19 route + opengraph-image, `/dev/ui` yok), `tsc` ✅, `eslint .` ✅. Prod (`next start`) uçtan uca: kayıt (yeni + dup-hata) → giriş (kayıtlı + kayıtsız-hata) → demo buton → builder → sepet → 4-adım checkout → sipariş → takip → admin demo geçidi. **Hiçbir network isteği yok**, gömülü kimlik yok, `example.com` yok. Konsol: FRESH tab'da tüm route'ların hard-load'u + soft-nav'lar TEMİZ (#418 çıkmıyor); önceki #418/500'ler zombie-server (port çakışması) + biriken konsol buffer artefaktıydı. Kalan: "preloaded but not used" CSS uyarıları (hata değil — page CSS'i client component'te import ediliyor; TASK-244b). **FAZ A COMPLETE.** | ✅ / ✅ | — |

## FAZ B — Backend-ready data layer

| Task | Tarih | Özet | build/tsc/lint | Not |
|---|---|---|---|---|
| TASK-213..220 | 2026-09-03 | **Tüm `localStorage` erişimi tek noktaya toplandı.** `services/adapters/local/storage.ts` (readRaw/writeRaw/removeRaw/readJSON/writeJSON/emit/subscribe) = tarayıcı depolamasına TEK erişim. `lib/{cart,orders,session,community,checkout}.ts` bu helper'a taşındı (doğrudan `window.localStorage` yok). Yeni domain modülleri: `lib/reviews.ts` (menü yorumları), `lib/gallery.ts` (`coffees` — tasarlanan kahveler), `lib/builder-draft.ts` (`copiedRecipe` aktarımı), `lib/profile.ts` (userPosts/avatar/bio), `lib/catalog.ts` (admin ürünleri), `community.ts`+post/vote/champion helper'ları. **4 client component** (`MenuClient`, `KahveniOlusturClient`, `ToplulukClient`, `ProfilClient`, `AdminPanelClient`) raw `localStorage` → domain modülü çağrısı. `grep -rn localStorage app/ components/ lib/ \| grep -v adapters/local/storage` = **0** (yalnız `types.ts` yorumu). | ✅ / ✅ | Prod: menü→sepet→checkout→sipariş, yorum ekleme, profil sekmeleri (sipariş görünür), topluluk (kart+sıralama+oy okuma) — hepsi yeni katmandan çalışıyor. |
| TASK-221 | 2026-09-03 | `lib/services/` arayüz dosyaları: `auth.ts` (AuthService interface + local adapter), `catalog.ts` (ProductService), `cart.ts` / `orders.ts` (re-export + sözleşme yorumu), `index.ts` barrel (`authService`, `productService`, `cartService`, `orderService`). `docs/BACKEND_CONTRACT.md` (yeni): katman diyagramı, "mock → API 3 adımda geçiş" rehberi, her servis için metod imzası + önerilen REST endpoint + hata sözleşmesi. | ✅ / ✅ | "Yarın API gelince ne değişir?" cevabı: yalnız `adapters/http/*` + 1 satır export değişimi. |
| TASK-222 | 2026-09-03 | **FAZ B regresyon checkpoint.** `next build` ✅ · `tsc` ✅ · `eslint .` ✅ (0). `localStorage` yalnız `storage.ts`'te. Tüm veri akışları (sepet, sipariş, checkout son-adres, yorum, profil, galeri, topluluk, admin) prod'da doğrulandı. **FAZ B COMPLETE.** | ✅ / ✅ | — |

## FAZ C — Conversion / UX friction

| Task | Tarih | Özet | build/tsc/lint | Not |
|---|---|---|---|---|
| TASK-223 | 2026-09-03 | Builder keşfedilebilir: `lib/nav.ts` "Kahveni Oluştur" `requiresAuth` kaldırıldı — logged-out kullanıcı nav'dan görür. "Sepete ekle" hâlâ giriş ister; **girişe giderken tasarım `saveBuilderDraft` ile kaydedilir**, dönünce form + isim geri yüklenir (kilitsiz). Toast "Tasarımın kaydedildi." | ✅ / ✅ | Prod: logged-out → builder → tasarla → sepete ekle → giriş → dönüş → tasarım korunuyor. |
| TASK-224 | 2026-09-03 | Checkout özet paneline **kalem listesi** (`<details open>` "N ürün" — ad ×adet · tutar). 4 adımın hepsinde görünür/daraltılabilir. | ✅ / ✅ | Prod'da doğrulandı. |
| TASK-225 | 2026-09-03 | Özet paneline güven satırı: "Bilgilerin yalnızca bu sipariş için kullanılır. Demo ortamı — gerçek ödeme alınmaz." | ✅ / ✅ | — |
| TASK-226 | 2026-09-03 | Sepet özeti: "Standart teslimat" her zaman satır olarak (eşik altı `₺30`, üstü **"Ücretsiz"** yeşil). Eşik üstünde `✓ Ücretsiz kargoyu kazandın` (success). "Toplam" artık ara toplam + tahmini standart kargoyu yansıtıyor + "Hızlı seçersen ödeme adımında güncellenir" notu. | ✅ / ✅ | Prod: eşik üstü pozitif durum doğrulandı. |
| TASK-227 | 2026-09-03 | Sepet kart yoğunluğu: thumb 80→64px (mobil 56), satır gap 16→12, `padding-block` küçüldü; ad `text-overflow:ellipsis`. Recipe satırı "S" harfi → `Coffee` lucide ikonu + tint zemin. | ✅ / ✅ | Prod: daha yoğun, "bitmiş" görünüm. |
| TASK-228 | 2026-09-03 | Sipariş onayı: lede + **adres teyidi** ("Kadıköy / İstanbul adresine") + **tahmini teslim tarihi** (`estimateDelivery` — standart +2–3 gün, hızlı +1). `lib/format.estimateDelivery` (yeni). CTA sadeleşti (Siparişlerim kaldırıldı — takip + alışveriş). | ✅ / ✅ | Prod: "Tahmini teslim: 5–6 Eylül". |
| TASK-242 | 2026-09-03 | Nav ikonları: `Topluluk` Trophy→**Users**, `Kahveni Oluştur` FlaskConical→**SlidersHorizontal** (yarışma/lab çağrışımı kırıldı). | ✅ / ✅ | — |
| TASK-252 | 2026-09-03 | Onay ekranına 1 satır demo notu: "Bu bir demo siparişidir — gerçek ödeme alınmaz, kargo çıkışı yapılmaz." | ✅ / ✅ | — |
| TASK-237 | 2026-09-03 | **Ana sayfa hero yeniden tasarlandı.** Ortalı-metin → asimetrik 2 kolon: solda eyebrow + h1 + lede + CTA, sağda güçlü ürün fotoğrafı (`espresso.jpg`, `fill` + `priority`, `aspect-ratio 5/6` desktop / `4/3` mobil, `object-position center 40%`, `--r-lg` + `--shadow-2`). Mobilde metin → görsel dikey akış. Katlama boşluğu azaldı (`padding-block s-9 → s-8/s-6`). Yeni efekt (gradient/glow/animasyon) YOK. | ✅ / ✅ | Prod desktop + 375: hero "gerçek mağaza" hissi; 5-sn testi geçiyor. |
| TASK-249 | 2026-09-03 | Hero copy somutlaştı: eyebrow boş slogan ("Kahvenin en samimi hali") → **"İstanbul'da kavrulur · 2–3 günde kapında"** (teslimat vaadiyle tutarlı, doğrulanabilir). Lede kısaldı ("ustalıkla kavurma" jargonu gitti): "Menüden hazır bir kahve seç ya da süt, çekirdek ve şurubu kendin ayarla. Siparişe göre taze kavrulur, kapına gelir." `BRAND.tagline` footer/OG'de kalır. | ✅ / ✅ | — |
| TASK-239 | 2026-09-03 | **Footer ticari yapıya geçti.** Tek kolon ortalı → 3 kolon grid: marka (ad Fraunces + tagline + tıklanır e-posta + adres) · KEŞFET (Menü/Kahveni Oluştur/Topluluk) · KURUMSAL (Hakkımızda + 4 yasal sayfa). Alt bar: © + "Bu bir portföy / demo projesidir." (`BRAND.isDemo`). Responsive: 720px'de 2 kolon, 440px'de 1. | ✅ / ✅ | Prod'da doğrulandı. |
| TASK-240 | 2026-09-03 | Yasal / kurumsal stub sayfaları: `/iade-teslimat`, `/mesafeli-satis`, `/kvkk`, `/gizlilik` (yeni). `components/LegalDoc.tsx` ortak bileşen (başlık + güncelleme tarihi + **görünür demo uyarısı** + numaralı bölümler) + `styles/legal.css`. İçerik gerçek TR e-ticaret belge yapısında (lorem değil), her sayfada "portföy/demo, bağlayıcı değil" notu. Metadata + `canonical` + `robots index`. `sitemap.ts`'e eklendi. | ✅ / ✅ | 4 route static prerender. |
| TASK-231 | 2026-09-03 | Boş durum ikonları: `/siparislerim` EmptyState → `Package` ikonu + açıklama ("İlk siparişini verdiğinde durumu buradan takip edebilirsin"). `/menu` "kategoride ürün yok" → `SearchX` ikonu + açıklama. | ✅ / ✅ / ✅ | Prod build ✅. |
| TASK-232 | 2026-09-03 | `ToastProvider` opsiyonel `action: {label, href}` desteği (aksiyonlu toast 3s→5s). `.toast-body`/`.toast-action` CSS. Menüde "Sepete ekle" → toast'ta **"Sepete git"** mikro-CTA. Builder zaten `/sepet`'e yönlendirdiği için orada eklenmedi. | ✅ / ✅ / ✅ | Prod'da doğrulandı: toast + tıklanır "Sepete git". |
| TASK-233 | 2026-09-03 | Profil istatistik bloğu: sıfır kullanıcıda **"0 0 0" duvarı yerine** tek satır yönlendirme ("Henüz bir hareket yok. Kahveni tasarlayarak başla" → `/kahveniolustur`). Hareket varken blok: Kahve · Gönderi · **Sipariş** (anlamsız "Puan"/arenaScore yerine gerçek sipariş sayısı). `.profil-stats-empty` CSS. | ✅ / ✅ / ✅ | Prod 375: sıfır kullanıcıda yönlendirme satırı doğrulandı. |
| TASK-234 | 2026-09-03 | Header oturum görünürlüğü: `NavDrawer`'a `isLoggedIn` + `onLogout` prop; giriş yapılıysa drawer'ın en altında **"Çıkış yap"** butonu (`margin-top:auto`, ayırıcı border). `HeaderNav` `clearSession` + `router.push("/")`. | ✅ / ✅ / ✅ | Prod: giriş→drawer→çıkış → session temizleniyor, `/`'a dönüyor. Logged-out'ta buton yok. |
| TASK-243 | 2026-09-03 | Drawer ayrımı: scrim `rgba(0,0,0,.62)`, panel `--surface-2` + `border-left --line-strong` + belirgin gölge + padding. Üstte `.nav-drawer-brand` ("Elmenes Coffee", Fraunces, alt border). | ✅ / ✅ / ✅ | Prod: drawer arka plandan net ayrışıyor; marka satırı görünür. NOT: varsayılan avatar `profilikon.png` koyu zeminde düşük kontrast — TASK-246'da ele al. |
| TASK-230 | 2026-09-03 | Menü kartı tek birincil aksiyon: görsel + başlık artık tek `.menu-card-open` butonu (detay modalını açar, `:focus-visible` outline + başlık hover→accent). Kart footundaki "İncele" (ghost) kaldırıldı; yalnız `block` "Sepete ekle". `.menu-card-actions` CSS (grid + mobil kolon) silindi. | ✅ / ✅ / ✅ | Prod: 9 kartın her birinde 1 buton; ızgarada accent belirgin şekilde sakinleşti; görsel/başlık tıklayınca modal açılıyor. |
| TASK-229 | 2026-09-03 | `/siparis/[id]` dikey boşluk: `padding-block` alt `--s-9`→`--s-7`, h1 alt boşluğu `--s-6`→`--s-5`. Sayfa sonuna kapanış aksiyon satırı (`.siparis-detay-actions` — "Siparişlerim" secondary + "Alışverişe devam" ghost). | ✅ / ✅ / ✅ | Prod: aksiyon satırı→footer arası ~112px; sayfa "bitmiş" görünüyor, dev boşluk yok. |
| TASK-235 | 2026-09-03 | 5-sn testi (copy): hero lede kısaldı — "süt, çekirdek ve şurubu kendin ayarla" ayrıntısı (ikinci CTA zaten söylüyor) çıkarıldı → "Menüden hazır seç ya da kendi kahveni tasarla. Siparişe göre taze kavrulur, kapına gelir." Mobilde ≤2 satır; CTA katlamada. | ✅ / ✅ / ✅ | "Menüye göz at" birincil, "Kendi kahveni tasarla" secondary korundu. |
| TASK-238 | 2026-09-03 | Ana sayfa ritmi: eşit ağırlıklı 2-kart `home-split` bloğu kaldırıldı. "Kendi kahveni tasarla" tam genişlik `.home-feature` kartı; "Topluluk seçkisi" kartı → tek satır ince `.home-community-strip` (ikincil, `ink-3` + accent link). "Öne çıkanlar"a 1 satır bağlam (`.home-section-sub` "Menünün en çok tercih edilen dördü"). Yeni bölüm/efekt YOK. | ✅ / ✅ / ✅ | Ana sayfa artık 2 anlamlı h2 bölümü + trust şeridi; "template" ritmi kırıldı. |
| TASK-245 | 2026-09-03 | Kart görsel oranı: `.menu-card-img` ve `.home-product-image` zaten `aspect-ratio 4/3` + `object-fit cover` — tutarlılık doğrulandı, ek değişiklik gerekmedi. `/menu/[slug]` TASK-241'de aynı orana oturacak. | ✅ | — |
| TASK-246 | 2026-09-03 | Topluluk "Ödüller" clip-art PNG'leri (`/arena-gift.png`, `/kupon.png`, `/bardak.png`) → lucide `Gift`/`Ticket`/`Coffee` (tint zeminli `.community-reward-icon`). 3 kullanılmayan asset silindi. | ✅ / ✅ / ✅ | "clip-art / gamification" hissi azaldı. |
| TASK-247 | 2026-09-03 | Buton hiyerarşisi: menü ızgarasında kart başına "Sepete ekle" `primary` → `secondary` (outline). Artık hiçbir görünümde 3+ dolu-accent buton yan yana yok; ızgarada tek dolu-accent öğe kategori sekmesi. Detay modalındaki "Sepete ekle" `primary` kaldı (o görünümde tek birincil). | ✅ / ✅ / ✅ | Prod: menü ızgarası accent'i belirgin sakinleşti. |
| TASK-241 | 2026-09-03 | **Ürün detay `/menu/[slug]` route (SEO + paylaşılabilir).** `app/menu/[slug]/page.tsx` — `generateStaticParams` (23 ürün, `dynamicParams=false`), `async generateMetadata` (title/description/canonical/OG=ürün görseli), `Product` JSON-LD (`offers` fiyat TRY / InStock). `UrunDetayClient.tsx` — büyük görsel + Fraunces ad + açıklama + `RoastMeter`/`OriginTag`/`TastingNotes` + `QuantityStepper` + "Sepete ekle" (adetli, "Sepete git" toast) + teslimat notu; altında yorum akışı (modal içeriği buraya taşındı, `menuYorumlar` anahtarı korundu). **Modal kaldırıldı** — `MenuClient` sadeleşti (StarRating/review state/handlers/`Modal`/`Textarea` importları silindi), kart görsel+başlık → `<Link href="/menu/[slug]">`. Home "Öne çıkanlar" kartları da slug'a bağlandı. `sitemap.ts`'e 23 ürün URL'i. **Next 16 notu:** `params` artık `Promise` — `await params` şart; sync erişim sessizce `undefined` verip metadata'yı "Ürün bulunamadı" yapıyordu (yakalandı & düzeltildi). | ✅ / ✅ / ✅ | Build 24→47 route (23 SSG ürün). Prod: `/menu/latte` title/canonical/JSON-LD (`price:110.00`) doğru; sepete ekle (adet 1 & 2) + yorum ekleme çalışıyor; `/menu/nonexistent`→404; menü ızgarası modalsız, kartlar slug'a gidiyor. Konsol temiz. |
| TASK-250 | 2026-09-03 | Microcopy geçişi: `SITE_DESCRIPTION` (site geneli `<meta>` + Twitter) "Nitelikli çekirdek, ustalıkla kavurma…" jargonu → "Taze kavrulmuş kahve, kapına gelir. Menüden hazır bir kahve seç ya da kendi kahveni tasarla." (tagline + hero ile tek ses). OG görsel alt satırı "Menüden sipariş ver…" → "Menüden hazır seç…". Checkout adımları/onay/toast'lar tarandı: "Siparişin alındı." (❌ "ödemeniz çekildi" yok), ödeme adımında tek net demo notu — hepsi tutarlı. **"63 DD" format bug'ı: YOK** — `DELIVERY_LABEL` "Standart (2–3 gün)" / "Hızlı (1 gün)" temiz; kart "AA/YY" doğru TR biçimi. | ✅ / ✅ / ✅ | — |
| TASK-251 | 2026-09-03 | `BRAND` tutarlılığı: tek e-posta `merhaba@elmenescoffee.com` (footer + 3 yasal sayfa + mesafeli-satış aynı), tek adres "İstanbul, Türkiye", `phone`/`instagram` boş (footer'da gösterilmez). `SITE_DESCRIPTION` TASK-250 ile hizalandı. Kod yorumu zaten "portföy/demo, iletişim örnek amaçlı" diyor. | ✅ | Site genelinde tek marka bilgisi. |
| TASK-253 | 2026-09-03 | `hakkimizda`: jenerik "kahvenin topraktan fincana uzanan yolculuğuna saygı duyan küçük bir kavurucu" → somut ("İstanbul'da küçük partiler hâlinde kavuran bir dükkân; menüde hazır seçenekler + kendi tarifin; her sipariş sonrası taze hazırlanır"). Diskret `.about-demo-note` ("portföy / demo projesidir; sipariş/ödeme/teslimat gerçek değil"). metadata description de-jargon. 4 değer kartı zaten ~eşit uzunlukta + somut (origin adları, "küçük parti", "aynı gün yola çıkarıyoruz") — doğrulandı. | ✅ / ✅ / ✅ | — |
| TASK-254 | 2026-09-03 | **FAZ E regresyon checkpoint.** `next build` ✅ (47 route) · `tsc` ✅ · `eslint` ✅. `grep "ustalıkla kavurma\|topraktan fincana\|en samimi hali\|lorem\|TODO\|FIXME"` app/components/lib → **0**. Prod: hakkimizda somut + demo notu; home `<meta description>` yeni ses. **FAZ E COMPLETE.** | ✅ / ✅ / ✅ | — |
| TASK-244 | 2026-09-03 | Dikey ritim: sayfa alt `padding-block` tutarsızlığı giderildi — `adminpanel/hakkimizda/menu/odeme/profil/topluluk` (`--s-9`→`--s-8`), `home` (`padding-bottom --s-9`→`--s-8`), `kahveniolustur` (`--s-9`→`--s-8`). Artık tüm içerik sayfaları `--s-7 → --s-8` blok padding + footer `margin-top --s-8` = tekdüze footer-öncesi boşluk. Auth/loading/404 kasıtlı `min-height` merkezi düzenleri korundu (bunlar "bitmemiş" değil). | ✅ / ✅ / ✅ | Prod: home footer doc-bottom'a yaslı, footer sonrası boşluk 0. |
| TASK-248 | 2026-09-03 | **FAZ D regresyon checkpoint.** `next build` ✅ (47 route) · `tsc` ✅ · `eslint` ✅. Prod görsel geçiş: `/` (2 h2 bölüm + feature kart + topluluk şeridi + trust), `/menu` (modalsız, secondary butonlar, slug linkleri), `/menu/[slug]` (SEO + JSON-LD + sepete ekle + yorum), footer (3 kolon + demo notu), drawer (marka satırı + çıkış), yasal sayfalar (200). **Efekt denetimi:** `grep linear-gradient/radial-gradient/@keyframes` → yalnız önceden var olan işlevsel `toast-in`; yeni gradient/glow/animasyon **0**. **FAZ D COMPLETE.** | ✅ / ✅ / ✅ | — |
| TASK-236 | 2026-09-03 | **FAZ C regresyon checkpoint.** `next build` ✅ · `tsc` ✅ · `eslint` ✅. Temiz tab prod smoke: `/` (hero 2-kolon + 2 satır lede) → `/menu` (kart başına 1 buton, görsel/başlık→modal) → toast "Sepete git" → `/sepet` (ücretsiz-kargo ilerleme + not) → `/odeme` (özet kalemleri + demo güven satırı) → `/siparis?o=` (adres + "Tahmini teslim: 5–6 Eylül" + demo notu) → `/siparis/[id]` (adres + kapanış aksiyonları). **REGRESYON BULUNDU & ÇÖZÜLDÜ:** `/siparis` + `/siparis/[id]` hard-load'da React #418 (hidrasyon uyuşmazlığı) — sipariş verisi localStorage'dan gelir, SSR'da "bulunamadı" render edip istemcide dolu render ediyordu. Fix: her iki client'a `useSyncExternalStore` `hydrated` kapısı + `LoadingState` iskeleti (`SiparislerimClient` deseniyle aynı). Temiz tab'da 4 route (dolu / bulunamadı / onay / takip) — **konsol tamamen temiz, #418 YOK**. **FAZ C COMPLETE.** | ✅ / ✅ / ✅ | Not: 3106'daki eski tab'da görülen #418 birikmiş buffer artefaktıydı; fresh tab'da /odeme, /menu, / temiz. |
